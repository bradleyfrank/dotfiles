---
- name: MacOS Workstation
  hosts: localhost
  vars:
    desktop: "{{ ansible_distribution }}"
    homedir: "{{ ansible_env.HOME }}"
    usr_path: "/usr/local"
    shell: /usr/local/bin/zsh
    vscode: '/usr/local/bin/code'
    vscode_config: "{{ homedir }}/Library/Application Support/Code"
  roles:
    - packages
    - dotfiles
    - desktop