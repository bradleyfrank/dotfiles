---
# tasks file for dotfiles

- name: Create ~/Development folder structure
  file:
    path: "{{ ansible_env.HOME }}/Development/{{ item }}"
    state: directory
    mode: '0755'
  with_items: ['Projects', 'Build', 'Scratch']

- name: Create shared dotfile directories
  file:
    path: "{{ ansible_env.HOME }}/{{ item.path }}"
    state: directory
    mode: '0755'
  with_filetree: shared/
  when: item.state == 'directory'
  tags:
    - dotfiles

- name: Apply shared dotfiles
  copy:
    src: "{{ item.src }}"
    dest: "{{ ansible_env.HOME }}/{{ item.path }}"
    mode: '0755'
  with_filetree: shared/
  when: item.state == 'file'
  tags:
    - dotfiles

- name: Apply home-only dotfiles
  copy:
    src: "{{ item.src }}"
    dest: "{{ ansible_env.HOME }}/{{ item.path }}"
  with_filetree: home_only/
  when: item.state == 'file'
  tags:
    - dotfiles
    - home_only

- name: Apply VSCode dotfiles
  copy:
    src: individual/vscode/
    dest: "{{ vscode_config }}"
    mode: '0755'
  tags:
    - dotfiles

- name: Set proper permissions on ~/.ssh
  file:
    path: "{{ ansible_env.HOME }}/.ssh"
    state: directory
    mode: '0700'
  tags:
    - dotfiles

- name: Set proper permissions on ~/.ssh/authorized_keys
  file:
    path: "{{ ansible_env.HOME }}/.ssh/authorized_keys"
    state: touch
    mode: '0600'
  tags:
    - dotfiles

- name: Set proper permissions on ~/.ssh/config
  file:
    path: "{{ ansible_env.HOME }}/.ssh/config"
    state: touch
    mode: '0644'
  tags:
    - dotfiles

- name: Create vim extensions directory path
  file:
    path: "{{ vim_extensions_path }}"
    state: directory
    mode: '0755'
  tags:
    - dotfiles

- name: Install vim-airline
  git:
    repo: https://github.com/vim-airline/vim-airline.git
    dest: "{{ vim_extensions_path }}/vim-airline"
    depth: '1'
    version: "{{ vim_airline_version }}"
  tags:
    - dotfiles
    - packages

- name: Install vim-airline-themes
  git:
    repo: https://github.com/vim-airline/vim-airline-themes.git
    dest: "{{ vim_extensions_path }}/vim-airline-themes"
    depth: '1'
  tags:
    - dotfiles
    - packages

- name: Load shellrc variables
  include_vars: shellrc.yml

- name: Test .zshrc template
  template:
    src: "zshrc_{{ ansible_distribution }}.j2"
    dest: "{{ ansible_env.HOME }}/zshrc"
  tags:
    - dotfiles